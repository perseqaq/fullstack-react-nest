{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "client": "lerna exec --scope client -- yarn start",
    "server": "lerna exec --scope server -- yarn start"
  }
}
